#!/bin/bash

# Sleep is only for making sure that xorg starts normally 
# And thus the wallpaper will always display properly
# If it works without sleep, let it be that way
# sleep 0.5 && ~/.fehbg &
~/.fehbg &
